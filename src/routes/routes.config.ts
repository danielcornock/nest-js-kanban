export const story = '/stories';
export const storyId = '/:storyId';
export const storyNested = story + storyId;

export const board = '/boards';
export const boardId = '/:boardId';
export const boardNested = board + boardId;

export const column = '/columns';
export const columnId = '/:columnId';
export const columnNested = column + columnId;

export const boardConfig = '/boardConfig';
